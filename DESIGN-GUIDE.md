# Ocean Quest デザインガイドライン（正本）

**原器 = 水中ロボティクスQuest（/robotics）。** このページをFableで磨き切り、他領域Quest（洋上風力等）はこのガイドに従ってSonnetで横展開する。

## 運用ルール（最重要）

1. **ミネさんが/roboticsについて出した要望・FBは、対応と同時に必ずこのファイルへ追記する**（該当セクションへのルール化＋末尾の変更履歴）。
2. 横展開セッション（Sonnet）は、着手前にこのファイルを全文読むこと。ここに書かれたルールは実装で再発明しない。
3. このファイルの変更は人（ミネさん）のFB起点。AIが勝手に方針を変えない。
4. 横展開の下書き: `feature/offshore-wind-quest` ブランチにWIPあり（2026-07-08時点・未リリース）。

## 1. ページの型（章構成）

領域Questページは以下の順で構成する:

1. **Hero** — 誰向けの何のサイトかを一発で伝える＋要点3つ（AIO）
2. **01 WHY NOW** — なぜ今この領域か。数字バンド＋カード6枚＋出典
3. **02 JOB MAP** — 職種マップ8職種（必要スキル付き）〔白帯〕
4. **03 SKILL BRIDGE** — 異業種経験の活かし方8パターン
5. **04 SELF CHECK** — 蛇行タイムライン＋診断導線〔白帯〕
6. **05 PLAYER MAP** — 企業・機関マップ（公式リンク付き）
7. **06 BLOG & CONTENTS** — note最新3件＋コンテンツ導線〔白帯〕
8. **07 FAQ** — 3問（AIO・FAQPage JSON-LD）
9. **遊び心セクション** — 実寸比チャート等。番号なし・終盤に置く
10. **CTA** — 個人相談＋企業相談の2枚看板

実装は `app/robotics/page.tsx` を正とする（SectionHead・QuestFx・quest-* CSSを流用）。

## 2. 見出しシステム（45+型）

- 章番号: `<span class="qno">01</span>`（丸囲い）＋英字キッカー（letter-spacing広め）
- 背景に大きく薄い英字ゴーストテキスト（`.quest-ghost`。WHY NOW / SELF CHECK 等）
- h2は「主張」にする（説明文にしない）。例:「国が、2030年までのAUV産業化を宣言している。」
- h2の強調は `<em>` ＝ 色変え＋**オレンジのマーカー風下線**（`linear-gradient(transparent 68%, rgba(255,122,33,.5) 68%)`）
- 写真背景上の章は白文字系＋text-shadow、白帯上の章はインク色（`.quest-sec:not(.quest-sec-alt)` の切替が実装済み）

## 3. 余白リズム（ユーザーFBで確立・2026-07-08）

- `.section` 既定（上96px/下0）は使わない。**questページは上下対称**:
  - 通常セクション: `padding: 84px 24px`
  - 白帯セクション: `padding: 76px 48px` ＋ **前後に margin 40px**
  - 最終CTA: 下110px（フッター前の余白）
  - モバイル(≤720px): 56px / 48px+24px / 80px
- セクション同士が密着して見えたら余白の非対称を疑う（過去の不具合原因）

## 4. 背景・写真（ユーザーFBで確立・2026-07-08）

- **ページ背景の写真にオーバーレイ（薄い膜）をかけない。写真そのまま**。可読性は白カード・text-shadow・白帯で確保する
- /roboticsの背景 = 深海の沈没船写真 `public/images/backgrounds/deep-dive.jpg`（Unsplash素材・ミネさん支給）
- 白帯（`.quest-sec-alt`）は `rgba(251,255,253,0.82)`＋角丸24px。写真背景と交互に置いてリズムを作る
- 素材は権利がクリアなもののみ（Unsplash可）。政府資料内の写真（©JAMSTEC等）は転載不可

## 5. ヒーロー原則

- **オリジナルのアニメーションSVGシーン**を描き起こす（写真がなくても成立し、他サイトと被らない）
- ロード時から動く: /roboticsはAUV浮遊＋プロペラ回転＋ソナーリング＋ビーコン明滅＋ライトビーム＋海底スキャン＋気泡＋マリンスノー＋光の差し込み
- **ページの主張を背負う小物**を必ず1つ入れる（/robotics = 右端の深度メーター「-20m ダイバーの限界 / -200m AUVの現場」）
- 構図: テキスト左・シーン右。要点3つはガラスパネル（`.qhv-points`）
- 下部にSCROLLキュー。`prefers-reduced-motion` で全アニメ停止。モバイルはシーン減光＋メーター非表示
- バッジ: `.quest-badge`（ティールグラデのピル）で「◯◯ Quest」を明示。キッカーは「Ocean Quest Family」

## 6. コンテンツ原則

- **WHY NOWは政府一次資料ベース**。ローカルの `新規事業調査/海洋産業/政府公開海洋産業/` から数字を取る
  - 数字バンド（`.quest-stats-band`）: ダークタイル4枚・大きな数字はオレンジ・カウントアップ演出
  - 出典を必ず明記し、試算値には「一定の条件設定に基づく目安」の注記を付ける
  - カードは**6枚**。「なぜこの技術/産業でないといけないのか」の必然性を最低2枚入れる（物理的・技術的・社会的の三方向）
- **語彙**: 「翻訳できる」は使わない →「あなたの経験は、◯◯で武器になる。」（Skill Bridge）。「活かせる」も面白みがないので避ける
- **企業名を出すなら公式サイトへのリンクチップ必須**（`.quest-player-links`・別タブ）。「代表例で網羅ではない」と明記
- **遊び心セクション**: 実寸比のチャートで驚きを作る（/robotics = 潜航深度比較: ダイバー20m→富士山→しんかい6500→日本のAUV 8,000m→エベレスト→チャレンジャー海淵10,920m）。自虐的な注記で笑わせて締める（「ダイバーのバーが見えないのは間違いではありません」）
- 英単語の混入禁止（過去に「widen」混入事故）。日本語で書き切る

## 7. AIO / SEO

- metaタイトルは二段構え:「◯◯Quest | <検索意図に直結する説明句> | Ocean Quest」
- Hero直下に要点3つ（40〜70字の自己完結文）
- FAQ 3問＋FAQPage JSON-LD、WebPage JSON-LD（about付き）
- 新ページ公開時に **sitemap.ts と public/llms.txt に必ず追記**
- 狙うのは複合ワード（例:「海洋ロボット 求人」「洋上風力 転職」）。単体ビッグワードは狙わない

## 8. 導線（単一ソース）

- **Quest公開 = `lib/content.ts` の `publishedQuestAreas` に領域キーを1行追加**
  → ヘッダー「領域別Quest」ドロップダウン・フッター「領域別Quest」欄・診断結果のQuestリンクに自動反映
- 手でリンクを増やさない（メンテ漏れ防止）

## 8.5 OGP（SNS/Slackリンクプレビュー）

- **OGP画像は写真そのままにしない**。「何のサイトか」が一目で伝わるブランド合成画像にする（ユーザーFB・2026-07-09）
- 実装: `app/opengraph-image.tsx`（next/og・全ページ共通デフォルト）。構成 = ロゴ（左上）＋タグライン大文字＋提供内容チップ＋ドメイン＋「運営 POTENTIALIGHT」＋背景写真＋左からの可読性グラデーション
- フォントは `assets/fonts/ZenKakuGothicNew-*.ttf`（eBookカバーと共通）
- 領域Questページに個別OGPを作る場合も同じ型（ロゴ＋Quest名＋タグライン＋写真）で `app/<quest>/opengraph-image.tsx` を置く
- 注意: SlackはOGPをキャッシュする。更新確認は新しいチャンネル/メッセージで貼り直すか、Slackの場合はURL末尾に `?v=2` 等を付けて確認

## 9. フッター / ヘッダー

- フッター: ブランド欄（ロゴ＋会社名＋タグライン＋プライバシーポリシー・運営会社の小リンク）＋3カラム（コンテンツ/領域別Quest/ご相談）
- **フッターリンクは1行固定**（nowrap・モバイル解除）。長いラベルは短縮する（例: eBook（無料DL））
- ヘッダー「領域別Quest」はdetails/summaryのドロップダウン（クリック＋ホバー両対応・JS不要）

## 10. 検証チェックリスト（リリース前）

- [ ] `npx tsc --noEmit` PASS（devサーバー稼働中に `npm run build` は実行しない）
- [ ] プレビューで全章描画・コンソールエラーなし
- [ ] 余白: セクション間ギャップ40px均一・上下padding対称（実測）
- [ ] アニメ発火: 蛇行タイムライン・カウントアップ・rv出現
- [ ] モバイル幅で確認（翻訳表の縦積み・ヒーロー可読性・フッター）
- [ ] フッターリンクの描画高さ=1行
- [ ] sitemap / llms.txt / publishedQuestAreas の追記漏れなし
- [ ] PRは自分でマージしない（ユーザー承認後にマージ→本番反映をcurlで確認）

## 変更履歴

- 2026-07-09 OGPルール追加（写真だけのプレビュー禁止→ロゴ+タグライン合成。app/opengraph-image.tsx）。
- 2026-07-08 初版。/robotics構築時のユーザーFBを全て反映（余白リズム・背景オーバーレイ禁止・「武器になる」語彙・6カード化・企業リンク・深度チャート・AUVヒーロー・領域別Questドロップダウン・原器/横展開方針）。
